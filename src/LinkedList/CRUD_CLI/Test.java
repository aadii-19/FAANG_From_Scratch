package LinkedList.CRUD_CLI;
import LinkedList.Node;
public class Test {
    public static void main(String[] args) {
        Node node = new Node(30); // Using the node class Imported from node
    }
}
